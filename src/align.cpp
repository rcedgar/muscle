#include "muscle.h"

static const uint MAX_PROBCONS_SEQS = 500;

void cmd_align()
	{
	Die("TODO");
	//const string &InputFileName = opt(align);
	//MultiSequence InputSeqs;
	//InputSeqs.FromFASTA(InputFileName);
	//bool IsNucleo = InputSeqs.GuessIsNucleo();
	//if (IsNucleo)
	//	Warning("Input appears to be {ACGT/U}, input must be amino acids in this build");
	//ProgressLogInputSummary(InputFileName, InputSeqs);

	//const uint SeqCount = InputSeqs.GetSeqCount();
	//if (SeqCount <= MAX_PROBCONS_SEQS)
	//	RunMTProbcons(InputSeqs);
	//else
	//	RunSuper4(InputSeqs);
	}
